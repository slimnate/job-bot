/** Plus icon for primary “add / new” actions (pairs with `.btn-with-icon`). */
export function PlusIcon() {
  return (
    <svg className='btn-with-icon__icon' viewBox='0 0 16 16' fill='none' aria-hidden='true'>
      <path
        d='M8 3.5v9M3.5 8h9'
        stroke='currentColor'
        strokeWidth='1.5'
        strokeLinecap='round'
      />
    </svg>
  );
}
