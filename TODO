Make chrome worker use a profile or something similar so that the worker does not have to log in to linkedin every time. We are sometimes hitting a captcha check that will block the worker without manual intervention.

[x] Archive postings with good/bad labels; filter archived on /postings (active / archived / good / bad).
Use archived good postings to revisit employers — filter by company on active list.

Use archived good/bad postings to inform future rankings (inject examples into buildRankingPrompt; app settings toggle).

Application status tracking. "Starred" feature to mark postings that are important. Add status field to postings such as: want to apply, applied, wont apply, rejected, etc.

Notifications

Optimize README for human consumption

Wizard to create a scoring rubric for any generic type of job the user could want

Review system prompts and make modifications as necessary

Examine tests created, document test suite, make sure tests (specifically linkedin related) aren't doing lots of unnecessary work.
