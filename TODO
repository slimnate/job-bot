Make chrome worker use a profile or something similar so that the worker does not have to log in to linkedin every time. We are sometimes hitting a captcha check that will block the worker without manual intervention.

Create recurring schedule entries for workers.

Examine tests created, document test suite, make sure tests (specifically linkedin related) aren't doing lots of unnecessary work.

Add ability to archive postings, and a page to view archived postings. I may want to periodically examine archived postings that were a good fit to see if there are more new postings from the same employer that I can check on. Maybe add options to archive with a status of "good" or "bad" so that I can filter archived postings as well, and potentially use the archived postings to inform future rankings.

Notifications



on the workers page queue dialog, lets show the user what the default evaluator for the source is in the dropdown.
- If the source has an evaluator: "Source default (<preset name>)"
- If the source does not have an evaluator: "Source default (none)" -

